name: dicoding sistem ML pipeline CI

on: 
  push:
    branches: ["main"]
jobs:
  development-checks:
    runs-on: ubuntu-latest

    permissions:
      contents: write
    
    steps:
    # clones repository files to github VM
      - name: clone repository code
        uses: actions/checkout@v4

    # installs all dependencies
      - name: Install project dependencies on miniconda and conda environment
        uses: conda-incubator/setup-miniconda@v3
        with: 
          activate-environment: mlflow-env
          environment-file: workflow-ci/mlproject/conda.yaml
          auto-activate-base: false

      - name: run modelling script
        shell: bash -el {0}
        run: |
          cd workflow-ci/mlproject
          python modelling.py

      - name: save mlruns to repo
        run: |
          git config --global user.name "${{ secrets.username }}"
          git config --global user.email "${{ secrets.email }}"
          git add workflow-ci/mlproject/mlruns/

          if ! git diff --cached --quiet; then
            git commit -m "save mlruns from CI run [skip ci]"
            git push origin main
          else
            echo "no new mlflow tracking changes detected. skipping commit"
          fi
      - name: Upload MLflow artifacts to GitHub
        uses: actions/upload-artifact@v4
        with:
          name: mlflow-tracking-data
          path: workflow-ci/mlproject/mlruns/

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker Image via MLflow
        shell: bash -el {0}
        run: |
          cd workflow-ci/mlproject
          
          # Baris di bawah ini secara otomatis mencari folder run ID terbaru di dalam mlruns/0
          LATEST_RUN=$(ls -t mlruns/0 | grep -v "meta.yaml" | head -n 1)
          MODEL_URI="mlruns/0/$LATEST_RUN/artifacts/model"
          
          echo "Building docker image using model from: $MODEL_URI"
          
          # Menjalankan perintah bawaan MLflow untuk build docker image
          mlflow models build-docker \
            -m "$MODEL_URI" \
            -n "${{ secrets.DOCKERHUB_USERNAME }}/dicoding-ml-pipeline:latest"

      - name: Push Image to Docker Hub
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/dicoding-ml-pipeline:latest
